# 93 — First-version Review Findings

Status: active · Last updated: 2026-08-27

This is the canonical backlog for findings discovered while reviewing the runnable demo against the numbered specs. `R1`–`R22` came from the first-version review; `R23`+ and `F15`/`F16` came from the 2026-08-27 spec-versus-implementation pass. Entries are closed only after the named test or evidence exists.

## In-scope fixes

| ID | Severity | Finding | Spec contract | Fix shape | Status |
| --- | --- | --- | --- | --- | --- |
| R1 | P1 | Local `run`/`stream` overwrite the non-retrieval response with an abstention | `12 § 3.1`, `12 § 5` | Preserve direct terminal response; pinned by `demo/tests/test_workflow.py:48` | Fixed |
| R2 | P1 | Preferred LangGraph adapter has no streaming surface, so installed UI loses answer streaming | `12 § 6`, `20 § 3` | Add adapter streaming; pinned by `demo/tests/test_workflow.py:103` | Fixed |
| R3 | P1 | Responses lack `query_id`/`session_id`; tabs cannot prove snapshot consistency | `10 § 6.1`, `12 § 3`, `20 § 5` | Add validated identity; pinned by `demo/tests/test_workflow.py:60` | Fixed |
| R4 | P1 | Stage latency values are fabricated from total elapsed time | `12 § 6`, `70 § 4.3` | Measure actual stages in `demo/src/agent_workshop_demo/workflow.py:446` | Fixed |
| R5 | P1 | Ingestion IDs depend on absolute checkout path and missing roots are silently accepted | `11 § 4`, `11 § 6` | Logical URI IDs; pinned by `demo/tests/test_ingestion_eval_memory.py:122` and `:146` | Fixed |
| R6 | P1 | Query filters and blank questions are not validated at the workflow boundary | `10 § 3.1`, `12 § 5`, `20 § 6` | Central validation; pinned by `demo/tests/test_workflow.py:92` | Fixed |
| R7 | P2 | Eval calls hit-rate `recall_at_k` and ignores per-question recall denominator | `70 § 4.2` | Per-case ratios; pinned by `demo/tests/test_ingestion_eval_memory.py:167` | Fixed |
| R8 | P2 | README points to archived requirements and overstates native Milvus/model behavior | `index.md § Source status`, `70 § 6` | Link numbered specs and distinguish deterministic fallback from verified native features | Fixed |
| R10 | P1 | Dependency failures surfaced without stage or query context | `12 § 5`, `20 § 6` | Wrap causes in `WorkflowStageError`; pinned by `demo/tests/test_workflow.py` | Fixed |
| R11 | P1 | The first version concatenates selected chunks instead of synthesizing a grounded answer with an LLM | `13 § 3–8` | Add an injected OpenAI Responses generator, citation guard, deterministic fallback and trace; pinned by `demo/tests/test_generation.py` and `demo/tests/test_workflow.py` | Fixed |
| R12 | P1 | Dense chunk/query vectors use a deterministic hash placeholder rather than a semantic embedding model | `10a`, `11 § 5` | Add an injected OpenAI Embeddings provider behind `dense_vector()` with fixed 1024 dimensions and no implicit network default; pinned by `demo/tests/test_embedding.py` | Fixed |
| R13 | P1 | `unknown` query classification vetoes relevant retrieved evidence | `12 § 5.1`, `12 § 5.7` | Grade query-to-chunk relevance independently of the teaching classifier; pinned by `demo/tests/test_workflow.py` | Fixed |
| R14 | P1 | The local streaming fallback leaks raw generation failures without stage/query context | `12 § 5`, `20 § 6` | Buffer validated chunks inside the measured stage and wrap failures consistently; pinned by `demo/tests/test_workflow.py` | Fixed |
| R15 | P1 | Offline eval ignores golden facts, reranked/selected recall and abstention correctness | `70 § 3–4` | Load both fixtures and report the complete metric set; pinned by `demo/tests/test_ingestion_eval_memory.py` | Fixed |
| R16 | P1 | Offline ingestion silently skips PDFs and unsupported document types | `11 § 3.1`, `11 § 6` | Parse unmanifested PDFs page-by-page and reject unhandled types explicitly; pinned by `demo/tests/test_ingestion_eval_memory.py` | Fixed |
| R17 | P1 | Streamlit exposed source/doc/department filters, so users performed knowledge routing instead of the Agent | `12 § 4`, `20 § 2–5` | Remove Search Controls and make registered tools own validated metadata filters; pinned by `demo/tests/test_agentic_tools.py` | Fixed |
| R18 | P1 | Workflow lacked permission gating, explicit tool selection, bounded decomposition, multi-hop provenance and terminal answer self-check | `12 § 2–7`, `70 § 4.1` | Add explicit LangGraph/local nodes, bounded tool plan and comparison fixture; pinned by `demo/tests/test_agentic_tools.py` and `demo/eval/questions.json:q004` | Fixed |
| R19 | P1 | `chunking-experiment-v1` compared only two retrieval-oriented profiles | `11 § 4`, `70 § 5`, D44 | Add three isolated 128/256/512-token profiles, end-to-end L1/usage/latency dimensions, semantic calibration status, constraint-first Pareto selection and reviewed artifact gate; pinned by `demo/tests/test_chunking_experiment.py` | Fixed |
| R20 | P1 | Planner had no explicit identity/rewrite/step-back/decompose result or scope-preserving validator | `12 § 5.3`, D45 | Add rule and strict optional provider transformers, protected-term validation, query roles, one-call bound and workflow trace; pinned by `demo/tests/test_query_transform.py` | Fixed |
| R21 | P1 | Workflow generated directly from selected chunks without a provenance-safe preparation stage | `12 § 5.8`, `13`, D46 | Add disabled/auto/selective/summary/extraction projection, exact support validation, one-batch call and atomic fallback with local/LangGraph parity; pinned by `demo/tests/test_context_compression.py` and workflow parity tests | Fixed |
| R22 | P1 | Eval diagnostics had no maintainable active portfolio, decision actions, evaluator budget or lifecycle | `70 § 2a`, `70 § 4.0`, D47 | Add strict `eval-metric-registry-v1`, registered scalar measurements, `rag-eval-v3` goal/guardrail/operational decision surface and compatible baseline checks; pinned by `demo/tests/test_eval_governance.py` and `demo/tests/test_eval_runner.py` | Fixed |
| R23 | P1 | Offline eval graded only the focused generation-context bound, so a legal exhaustive answer failed and the sixteen-context ceiling was never checked | `12 § 5.9`, `13 § 8`, `70 § 4.6` | Derive the bound from `retrieval_goal` with `MAX_EXHAUSTIVE_CONTEXTS` moved to `config.py`; keep the top-5 window for the gated `selected_context_recall_at_5`; pinned by `demo/tests/test_eval_runner.py` | Fixed |
| R24 | P2 | The answer generator had no registered fallback-reason set, so an injected generator could put a provider exception body into trace | `13 § 6`, `13 § 8` | Add `GENERATION_FALLBACK_REASONS` and check it in `validate_generation_result`; pinned by `demo/tests/test_generation.py` | Fixed |
| R25 | P1 | `_terminal_path_valid` asserted required stages only, so an abstention that projected a generation context graded clean | `70 § 4.6` | Split `_required_stages`/`_forbidden_stages` and report `forbidden_stage_present`; pinned by `demo/tests/test_eval_runner.py` | Fixed |
| R26 | P2 | Compression provenance fed only an aggregate rate and could not fail a case | `70 § 4.6` | Pass the flag into `_outcome_failures` as `compression_provenance_invalid`; pinned by `demo/tests/test_eval_runner.py` | Fixed |
| R27 | P1 | Fixtures could not assert transformation strategy, item roles or compression path, so `70 § 4.6`'s "与 fixture 一致" was graded as self-consistency only | `10 § 6.2`, `70 § 4.6` | Register five optional `expected_*` keys with closed enums and compare them in the trajectory and outcome layers; pinned by `demo/tests/test_eval_runner.py` | Fixed |
| R28 | P1 | `scenario` v1 could only express a permission override, so cache-hit and Memory state-change cases had no way to declare their pre-state | `70 § 3`, F12 | Add `scenario.prelude`, replayed through the production `stream()` path on one session; add q014/q015; pinned by `demo/tests/test_eval_runner.py` | Fixed |
| R29 | P2 | A cache hit was graded as citation-invalid and as retrieval recall 0, penalising the short-circuit `10c § 6` requires | `10c § 6`, `70 § 4.0a`, `70 § 4.6` | Require a surviving citation instead of a subset of this turn's selection, report `null` for the three retrieval recalls, and make `_CacheHitWorkflow` model the real empty-selection shape | Fixed |
| R30 | P2 | `70 § 3` wanted a reranker-fallback golden case but the scenario schema could not express provider injection | `70 § 3`, `12 § 5.6` | Add `scenario.reranker=fallback` (the credential-free `RERANKER=auto` build, not a double) plus `expected_reranker_fallback` graded in the trajectory layer; q016 pins it | Fixed |
| R31 | P1 | `70 § 3` required golden fixtures for selective-compression success, malformed-output fallback, `retry_exhausted` and `duplicate_retry_query`, none of which an offline deterministic run can produce | `70 § 3`, `70 § 4.6`, D53 | Route them to the contract layer `70 § 6` already assigns to model-backed paths, shorten the minimum set, and record D53 with the deterministic-compressor route for reversing it | Fixed |
| R32 | P1 | One module constant gated the single-strong-chunk exception for every reranker, comparing scores from implementations with incompatible scales | `12 § 5.7`, `70 § 4.1`, D54 | Declare `strong_single_evidence_threshold` on `Reranker`, read it from the ranking implementation and fail closed when undeclared; both shipped values stay `0.80` because the measurement supports no other; pinned by `demo/tests/test_workflow.py` | Fixed |

## Deferred findings

`F` ids belong to this backlog. A bare `D<n>` anywhere in this file always
means a decision in [`99-key-decisions.md`](./99-key-decisions.md), never a row below. Numbers are stable
and never reused; `F11` was withdrawn during the first review.

| ID | Severity | Finding | Why deferred | Required next step |
| --- | --- | --- | --- | --- |
| F1 | P1 | Real Milvus collection/index/insert/hybrid query round trip is not tested | Insert/query adapter has deterministic fake-client coverage, but exact-version server evidence still requires a running service | Execute `91 § Phase 0` capability matrix and update schema/index parameters from observed APIs |
| F2 | P2 | Binary-vector MinHash representation is experimental and semantic validity is unproven | P2 feature; spec explicitly gates it | Validate near-duplicate fixtures or keep checksum-only dedup |
| F3 | P2 | DINOv3 and model reranker are labels/fallbacks, not integrated models | P2/model selection remains unverified | Benchmark selected models and record dimensions/runtime in `docs/research/` |
| F4 | P3 | The installed experimental `ruff format` corrupts Python source into placeholder AST names | Tool version is external to this repo; all affected source was recovered and verified | Do not use this formatter version; upgrade/pin Ruff before enabling formatting automation |
| F5 | P3 | Full strict mypy is not clean: with `MYPYPATH=demo/src` and explicit package bases it reports 52 existing errors across 12 non-eval files; the unqualified documented command also sees duplicate/import-path issues | 本次新增的 eval governance、runner、CLI 与测试通过 targeted strict mypy；跨模块 typing debt 不属于 Phase 5 eval portfolio | 固定 Python/mypy/PyMilvus toolchain 与 repository mypy config，逐模块清零现有 typing debt，且不通过全局 ignore 弱化 core checks |
| F6 | P1 | A real configured OpenAI Responses round trip has not been executed | No API credential/model entitlement was available in this session; SDK 2.46.0 signatures and fake-client behavior are verified | Run an opt-in smoke query with `OPENAI_API_KEY` and `OPENAI_MODEL`, then record model, latency and citation-valid output without storing prompt or response bodies |
| F7 | P1 | A real configured OpenAI Embeddings round trip has not been executed | SDK 2.46.0 request signature and fake-client behavior are verified; the task does not authorize spending API quota with ambient credentials | Run an opt-in smoke with `EMBEDDING_PROVIDER=openai`, confirm a finite 1024-value response, then re-ingest the target collection before semantic queries |
| F8 | P2 | Selective-Memory consolidation commits the fact projection before its deterministic lifecycle event | The Phase 6 review fixed correctness/security P1s first; crash-safe multi-collection commit needs a journal/outbox contract rather than a partial retry patch | Add a replayable consolidation outbox and a fault-injection test spanning `selective_memory.py:_consolidate` fact/event writes |
| F9 | P2 | Selective-Memory has same-session clear but no bounded resumable physical cleanup for expired/tombstoned rows | Logical expiry/status already blocks recall; safe physical deletion requires an operator-facing cursor/progress contract | Specify and implement validated session/status/expiry cleanup batches with cross-session non-deletion tests |
| F10 | P2 | Milvus selective-memory listing bounds results only after iterator reads and fake-client parity does not prove full server ordering/filter semantics | Real Milvus remains Phase 0-gated and the current adapter preserves correctness for the bounded workshop corpus | Push limit/order to Milvus, share a local/Milvus contract suite, and execute an opt-in correction/conflict/expiry/isolation/clear/decay round trip |
| F13 | P1 | `demo/src/agent_workshop_demo/schema/evolution.py:524` 调用的 `pymilvus.milvus_client.index.extract_bound_index_param` 在公开 `pymilvus==3.0.1` 不存在 | 本次 Phase 5.1 未修改 schema evolution；全量 320-test gate 在正确 pin 下只因此失败（另有 1 个显式 opt-in smoke test skipped） | Phase 8 核对 3.0.1 的公开 index param API，替换私有/不存在 helper，并固定 `test_exact_sdk_request_enables_physical_backfill` |
| F17 | P2 | The `Milvus 3.0 Force Merge` answer cites `doc_milvus_feature_map_c001` alongside the Force Merge chunk, but `70 § 3` requires the fixture to cite only the live Force Merge chunk | `_is_strong_single_evidence` requires `len(relevant) == 1` and the question yields two: the Force Merge chunk at `0.7324` and an unrelated Retrieval Layer chunk at `0.5247`, which clears `RELEVANCE_THRESHOLD = 0.22` and passes `_has_query_overlap` on the shared token `Milvus` alone. The exception is therefore blocked before its score gate is consulted, and changing that gate would not affect this case | Decide whether relevance should require a distinctive-term overlap rather than any shared token; any change perturbs every question's evidence set, so it needs a comparative report under `70 § 4.2`, not a tuned constant |
| F18 | P3 | Policy-domain routing depends on surface wording: `TTL Memory Policy 是什么？` reaches `search_policy_docs`, while `安全策略对会话记忆保留有什么要求？` routes to `search_code_docs` | q013 pins the working phrasing; the routing rules are keyword-shaped and the security corpus is a single chunk, so the miss is not yet separable from thin evidence | Add security-domain corpus depth, then decide whether tool selection needs topic signals beyond keywords |
| F14 | P1 | 当前安装的 `pymilvus.FunctionType` 没有 `MINHASH`，使三个 collection/index schema tests 在 `schema/pymilvus_adapter.py:885` 失败 | Eval portfolio 变更未触及 schema adapter；本次全量 366-test gate 除这三个依赖兼容错误外均通过，另有两个显式 opt-in smoke tests skipped | Phase 8 将 MinHash function contract 与锁定的 Milvus/PyMilvus capability matrix 对齐；不支持时 fail closed 或关闭实验字段，并固定 collection/index contract tests |
