[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "agent-workshop-demo"
version = "0.1.0"
description = "Runnable Agentic RAG workshop demo"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]
