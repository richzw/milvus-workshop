"""Operational entrypoints for the workshop demo.

The package marker exists so type checking resolves these modules under one
stable name; every script remains runnable directly as ``python3
demo/scripts/<name>.py``.
"""
